// Package consumption smoke test. Grows with each generated capability; for now it proves the
// packages restore from the local feed, the analyzer loads, and the consumer compiles warning-free.
Console.WriteLine("Arrow.SourceGenerator package consumption: OK");
return 0;
