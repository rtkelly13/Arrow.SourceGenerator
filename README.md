# Arrow.SourceGenerator

Compile-time, reflection-free mapping between ordinary .NET models and Apache Arrow.

This repository is being bootstrapped through a series of foundation pull requests; see
`docs/00-DESIGN-GOALS.md` once the first one lands.
